package com.Game.util;

import com.Game.model.map.Map;

/**
 * Provides map validation functionality.
 */
public class MapValidator {
    /**
     * Validates that a map is a connected graph.
     * 
     * @param p_map The map to validate.
     * @return True if the map is valid, false otherwise.
     */
    public static boolean isMapConnected(Map p_map) {
        // Implementation
        return true;
    }
    
    /**
     * Validates that each continent is a connected subgraph.
     * 
     * @param p_map The map to validate.
     * @return True if all continents are connected, false otherwise.
     */
    public static boolean areContinentsConnected(Map p_map) {
        // Implementation
        return true;
    }

}