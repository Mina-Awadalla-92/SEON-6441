package com.Game.command;


/**
  * Since Command is an interface, you cannot directly write unit tests for it. 
  * Instead, you need to test classes that implement it. The Command interface defines methods (execute, undo, * getCommandName, and *validate) that must be implemented by any class that implements this interface. 
 */

public class CommandTest {
    
}
