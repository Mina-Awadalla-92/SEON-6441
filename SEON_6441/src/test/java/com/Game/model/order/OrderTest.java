package com.Game.model.order;

/**
 * Since we already have a concrete subclass of Order (like DeployOrder) that implements the execute() method, 
* we can directly write unit tests for DeployOrder. This approach allows us to test both the functionality of 
* the DeployOrder class and the inherited methods from the Order class without needing to create a separate * *
* simple subclass.
*/

public class OrderTest {
    
}
